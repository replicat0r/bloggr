<h2>sign In</h2>

<%= form_for @user, :url => sessions_url do |f| %>
	<%= f.label :email %>
	<%= f.email_field :email %>
	<%= f.label :password %>
	<%= f.password_field :password %>
	<%= f.submit "Sign In" %>
	<% end %>
		<%= link_to("New User?", user_sign_up_url ) %>
